#include <stdio.h>
#include <stdlib.h>

int main(){
    int i,j, n, h, ds, req[100];
    printf("Enter the number of request: ");
    scanf("%d", &n);
    for(i=0;i<n;i++){
        printf("Enter the queue: " );
        scnaf("%d",&req[i]);
    }
    printf("Enter the currunt head p ")

}